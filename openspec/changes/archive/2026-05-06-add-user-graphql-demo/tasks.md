## 1. 后端 GraphQL 基础搭建

- [x] 1.1 在 `backend/` 安装并配置 NestJS GraphQL 所需依赖与应用启动配置
- [x] 1.2 启用后端本地 CORS，使前端开发服务可以访问 GraphQL 端点
- [x] 1.3 保留现有基础接口可用，同时暴露 GraphQL HTTP 端点

## 2. 用户模块与查询实现

- [x] 2.1 创建 `user` 模块并定义用户对象结构，包含 id、name、email 字段
- [x] 2.2 在用户服务中实现基于内存数据的用户列表查询与按 id 查询
- [x] 2.3 创建 GraphQL resolver，提供查询全部用户与查询单个用户的能力
- [x] 2.4 为不存在的用户查询返回空结果，并补充后端测试覆盖关键查询行为

## 3. 前端 GraphQL 示例页面

- [x] 3.1 初始化 Apollo Client，并在前端应用中提供 GraphQL 客户端上下文
- [x] 3.2 在前端引入最小路由系统，并新增 GraphQL demo 独立页面入口
- [x] 3.3 使用 Apollo Client 向后端 GraphQL 端点发起用户查询并渲染结果
- [x] 3.4 补充加载中、错误态与成功态展示，确保界面反馈完整

## 4. 联调与验证

- [x] 4.1 验证后端 GraphQL 查询可以返回预期用户数据
- [x] 4.2 启动前端并在浏览器中验证 demo 页面可以成功调用 GraphQL 接口
- [x] 4.3 运行必要的构建或测试命令，确认前后端改动可正常通过
