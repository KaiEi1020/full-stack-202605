## 1. Prisma 基础设施搭建

- [x] 1.1 在 `backend/` 引入 Prisma 依赖、初始化 schema 与生成客户端
- [x] 1.2 配置 Prisma 在 NestJS 中的生命周期管理与注入方式
- [x] 1.3 为现有用户数据建立最小可用的 Prisma 数据模型与开发期初始化方案

## 2. 后端双模式架构约束落地

- [x] 2.1 定义复杂业务采用 DDD、简单 CRUD 采用普通 service 的目录结构与模块边界
- [x] 2.2 为 GraphQL resolver 约定两种接入路径：应用层用例路径与普通 service 路径
- [x] 2.3 收敛共享基础设施，确保两种实现模式统一通过 Prisma 访问数据

## 3. 用户查询能力迁移

- [x] 3.1 将现有用户查询改造成基于 Prisma 的数据访问实现
- [x] 3.2 为用户模块判断当前场景应采用 DDD 还是普通 service，并完成对应重构
- [x] 3.3 调整用户 GraphQL resolver，使其通过选定模式返回用户列表与单个用户结果

## 4. 测试与验证

- [x] 4.1 为采用 DDD 的复杂路径补充领域层或应用层测试
- [x] 4.2 为简单 CRUD 路径补充 service 与 GraphQL 集成测试
- [x] 4.3 运行必要的后端构建与测试命令，验证 Prisma、GraphQL 与模块装配正常工作
