## 1. 后端数据与接口准备

- [x] 1.1 为用户 Prisma 模型增加手机号字段及所需约束，并更新生成产物
- [x] 1.2 扩展用户领域模型、GraphQL 输出模型和相关类型，使其包含手机号字段
- [x] 1.3 定义用户注册 GraphQL 输入与 `registerUser` mutation 契约

## 2. 后端注册流程实现

- [x] 2.1 在用户模块中新增注册服务，完成用户名和手机号校验、重复手机号检查与用户创建
- [x] 2.2 扩展用户仓储能力，支持按手机号查询和创建用户
- [x] 2.3 在 `UserResolver` 中接入注册 mutation，并返回注册结果

## 3. 消息通知链路接入

- [x] 3.1 在后端接入 RabbitMQ 消息发布边界，定义注册成功通知消息结构
- [x] 3.2 在注册成功后发布通知消息，消息体包含用户标识、用户名和手机号
- [x] 3.3 实现短信占位处理逻辑，使用打印方法模拟短信发送

## 4. 前端注册页面实现

- [x] 4.1 新增注册页面或注册区域，提供用户名和手机号输入表单
- [x] 4.2 新增前端 GraphQL mutation 调用逻辑，提交注册请求并处理响应
- [x] 4.3 在前端展示注册成功与失败反馈，并保证现有查询演示能力不被破坏

## 5. 验证与回归

- [x] 5.1 为后端注册服务或 resolver 补充测试，覆盖成功注册与重复手机号失败场景
- [x] 5.2 验证注册成功后消息发布与短信打印占位逻辑可工作
- [x] 5.3 运行前后端必要的构建与测试命令，确认注册链路可用
