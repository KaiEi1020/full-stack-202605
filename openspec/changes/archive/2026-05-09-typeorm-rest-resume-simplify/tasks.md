## 1. 基础设施切换

- [x] 1.1 清点并移除 backend 中的 Prisma 相关依赖、配置、生成物与启动接线
- [x] 1.2 引入 TypeORM 与 SQLite 所需依赖，并建立 NestJS 的 TypeORM 数据源配置
- [x] 1.3 建立基础实体目录、迁移目录与应用启动时的数据库装配方式

## 2. 简历单表建模

- [x] 2.1 梳理现有简历相关多表结构与字段，确定新的 resume 单表字段清单
- [x] 2.2 实现 resume TypeORM 实体与必要的列类型、索引及约束
- [x] 2.3 编写从旧简历多表结构收敛到单表的迁移脚本或数据库迁移
- [x] 2.4 为无法直接映射的旧字段定义保留、序列化或弃用规则并落地到迁移实现

## 3. REST 接口替换 GraphQL

- [x] 3.1 识别并移除现有 GraphQL schema、resolver、module 接线与相关依赖
- [x] 3.2 为简历资源实现 RESTful controller、DTO、service 与响应约定
- [x] 3.3 完成简历资源的创建、详情查询、更新与删除接口，并覆盖不存在与非法请求场景
- [x] 3.4 增加简历 AI 打分接口，返回技能匹配度、经验相关性、教育背景契合度、综合评分与 AI 评语
- [x] 3.5 将前端或其他调用方原有 GraphQL 调用改造为对应 REST 接口调用

## 4. 评分展示与交互

- [x] 4.1 设计并实现简历评分结果的数据展示视图
- [x] 4.2 实现评分图表组件，至少支持雷达图、柱状图或环形进度条中的一种展示形式
- [x] 4.3 在图表视图中同时展示 AI 评语，并保证各维度评分含义清晰可读

## 5. 模块架构重组

- [x] 5.1 盘点当前后端模块，明确哪些模块保留 DDD、哪些模块改为 CRUD
- [x] 5.2 对复杂模块保留或重建领域层、应用层、仓储边界与基础设施实现
- [x] 5.3 对简单模块重构为 controller + service + TypeORM Repository 的 CRUD 结构
- [x] 5.4 校验复杂模块与简单模块并存时的依赖方向与模块职责边界

## 6. 验证与收尾

- [x] 6.1 为 TypeORM 持久化、简历单表迁移、AI 打分能力与 REST 接口补充或更新测试
- [x] 6.2 验证 SQLite 下的数据初始化、迁移执行、评分结果生成与接口联调结果
- [x] 6.3 验证评分图表与 AI 评语在前端页面中的展示效果
- [x] 6.4 清理 Prisma 残留文件、无用抽象与失效文档，确保代码库与新架构一致
