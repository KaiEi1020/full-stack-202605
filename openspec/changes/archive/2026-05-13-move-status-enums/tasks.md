## 1. 识别与迁移枚举定义

- [x] 1.1 盘点 `backend/src/core/database/` 下的业务状态枚举及其当前引用点
- [x] 1.2 为 `CandidateStatus`、`JobStatus`、`ParseStatus` 选择目标业务目录或共享业务类型目录
- [x] 1.3 移动枚举文件并保留现有名称、成员和值不变

## 2. 更新代码引用与导出

- [x] 2.1 更新实体、DTO、service、orchestrator 对迁移后枚举的 import 路径
- [x] 2.2 更新相关 barrel 导出，移除 `core/database` 对业务状态枚举的导出
- [x] 2.3 检查并修正所有残余的旧路径引用，确保数据库层不再承载业务状态定义

## 3. 验证迁移结果

- [x] 3.1 运行后端构建或测试，验证迁移后编译与运行行为正常
- [x] 3.2 确认数据库字段值、API 语义与业务判断逻辑未因枚举迁移发生变化
