{
  "name": "full-stack-202605",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter backend test",
    "deploy:cvm:pack": "bash ./deploy-cvm.sh pack",
    "deploy:cvm:transfer": "bash ./deploy-cvm.sh transfer",
    "deploy:cvm": "bash ./deploy-cvm.sh all"
  },
  "packageManager": "pnpm@10.6.5",
  "pnpm": {
    "onlyBuiltDependencies": [
      "prisma",
      "better-sqlite3"
    ]
  }
}
